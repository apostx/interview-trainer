Drop a full alternate content bank here as a subfolder, e.g.
content/versions/<label>/*.json — each subfolder is one selectable
version in the Study dev-mode switcher (open Study with ?dev=1).
The live bank stays in content/packs/. See docs/content-authoring.md.
